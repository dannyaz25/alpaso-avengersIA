import { DefaultTheme } from 'styled-components';

export const theme: DefaultTheme = {
  colors: {
    primary: '#ffd700',
    secondary: '#1e40af',
    background: '#1a1a1a',
    text: '#ffffff'
  }
};

export default theme;
